# 更新日志

## 0.3.2 · 2026-09-17

- 编辑模型服务地址时不再随每次输入重复读取钥匙串，避免连续出现授权弹窗。
- 仅修改服务地址且 API Key 留空时，会继续使用已保存的 Key，并在保存时关联到新地址。
- 未修改 API Key 或 Webhook 地址时不再重复写入钥匙串。
- Webhook 凭据在同一次运行中只按需读取一次；读取失败后不会由后台操作反复触发授权，仅在再次主动打开设置时重试。
- 发布前自动化测试共 72 项通过，2 项可选渲染测试按设计跳过。

## 0.3.1 · 2026-09-15

- 报告区新增「阅读 / Markdown」切换；阅读模式按标题、列表和引用排版，不显示 Markdown 标记。
- 复制按钮和 `⇧⌘C` 跟随当前显示模式，TXT 导出改为无 Markdown 标记的简洁文本。
- 历史报告预览使用相同的阅读排版。
- API Key 输入框增加显隐按钮，可按需读取钥匙串中的已保存 Key 进行核对或修改。
- 默认仍隐藏 API Key；切换服务地址、清除 Key 或关闭窗口会退出明文显示。

## 0.3.0 · 2026-09-15

### 报告输出与历史

- 报告支持导出 Markdown `.md` 与文本文件 `.txt`。
- 保存最近 90 天由手动整理或智能总结生成的报告版本。
- 历史页支持关键词、时间、周期与来源筛选，以及预览、恢复、删除和再次导出。

### 通知与更新

- 可选 macOS 系统通知与 Webhook，在手动更新报告或智能总结成功后推送更新摘要。
- Webhook 地址存入 macOS 钥匙串，只发送标题、日期范围与统计数量，不发送报告正文。
- 侧栏增加手动检查更新与 GitHub 项目入口；检查结果跳转到应用内固定的可信下载地址。

### 大模型协议

- 智能总结切换到 OpenAI Responses 兼容接口，使用 `POST /responses` 和 `output_text`。
- 请求显式关闭流式输出与服务端存储：`stream: false`、`store: false`。
- 模型设置支持通过兼容的 `GET /models` 接口读取模型列表，同时保留手动填写模型 ID。
- 旧的完整 `/chat/completions` 地址会自动转换为 `/responses`。

### 安全与可靠性

- 模型、Webhook 与更新检查网络请求使用临时会话，不保留 Cookie 或响应缓存，并拒绝重定向。
- 更新检查处理 GitHub 限流、网络、超时和无效版本响应，不显示远端响应正文。
- 0.3.0 发布前自动化测试共 66 项，2 项可选渲染测试另行启用，测试结果为 0 失败。

## 0.2.0 · 2026-09-05

CommitBrief 首个公开分发版本，提供 macOS 15 及以上、Apple Silicon Mac 的 DMG 安装包。

### 从 Git 记录生成工作汇报

- 添加多个本地 Git 仓库，或扫描工作目录查找仓库。
- 按当天、最近 7 天、最近 30 天与最近 90 天整理已提交的工作。
- 按项目、模块和功能点查看工作项，展示原始提交以便核对。
- 支持自己的提交、自定义多个作者或全部作者；支持当前分支、全部分支与标签及合并提交选项。
- 手动修改描述与模块，排除或恢复工作记录。
- 编辑、预览、复制和导出 Markdown，按需附上提交编号。
- 本地保存项目设置与草稿；重新整理时保护手动草稿，并支持恢复上一份报告。
- 支持浅色、深色模式及常用快捷键。

### 使用自己的大模型

- 自定义 OpenAI Chat Completions 兼容服务地址、模型 ID 和 API Key。
- 提供简洁与详细两种中文汇报风格；支持兼容的本机模型服务。
- API Key 按接口分别保存在 macOS 钥匙串。
- 测试连接不发送 Git 记录；总结只在手动触发后发送当前纳入报告的数据。
- 生成过程支持取消；连接失败、返回截断或无效响应时保留原稿。
- 生成期间更改范围、编辑草稿或保存模型配置，会取消旧请求，避免旧响应覆盖新草稿。
- 单次完整 AI 请求限制为 300 KB，超限时明确提示，不静默删除工作记录。

### 分发说明

- 本仓库仅公开文档、素材和安装包，应用源码保持私有。
- 安装包使用 ad hoc 签名，尚未完成 Apple Developer ID 签名与公证。
- 当前为 arm64 版本，尚无 Intel 版或自动更新功能。
- AI 请求、错误处理与草稿保护经过模拟接口验证；未使用真实付费 Key 验证各服务商的生成质量。实际兼容性和输出质量取决于所选模型服务。

查看 [安装说明](README.md#下载与安装)、[大模型设置](docs/AI.md) 和 [下载页面](https://github.com/GpsLypy/CommitBrief/releases/tag/v0.2.0)。
